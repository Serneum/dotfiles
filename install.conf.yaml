- defaults:
    link:
      relink: true

- clean: ['~']

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  -
    command: if [[ $SHELL != "/bin/zsh" ]]; then sudo chsh -s /bin/zsh `whoami`; fi
    stdin: true
    stdout: true
  -
    command: if [[ $OSTYPE == darwin* ]] && ! which brew &>/dev/null; then /usr/bin/ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"; fi
    stdout: true
  -
    command: if [[ $OSTYPE == darwin* ]]; then brew install ag autojump cheat git keybase tree gpg python; fi
    stdout: true

    command: sudo pip install ./powerline
    stdout: true

- link:
    ~/.antigen.zsh: antigen/antigen.zsh
    ~/.bash: bash/bash
    ~/.bashrc: bash/bashrc
    ~/.gitignore: git/gitignore
    ~/.gitconfig: git/gitconfig
    ~/.iterm2: mac/iterm2
    #    ~/.oh-my-zsh: oh-my-zsh
    ~/.spacemacs: emacs/spacemacs
    ~/.tmate.conf: cmm/tmate.conf
    ~/.vimrc: vim/vimrc
    ~/.zsh: zsh
    ~/.zshrc:
      path: zsh/zshrc
      force: true
