A Git branch should always include a Jira ticket ID as a prefix. A branch name should look like PROJECT-XXX-description-of-work. For example, CBDEVOPS-1234-fix-bug-with-translator

New branches must always be created from the default branch (e.g. master or main). Before creating a new branch, check out the default branch and pull the latest changes first.
